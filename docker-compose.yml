version: "3.9"

services:
  backend:
    build: ./backend
    container_name: bfs-backend
    ports:
      - "3001:3000"
    env_file:
      - ./backend/.env

  frontend:
    build: ./frontend
    container_name: bfs-frontend
    ports:
      - "5173:5173"